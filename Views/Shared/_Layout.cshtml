<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>@ViewData["Title"] - EventEats Quotify</title>

    <!-- External Scripts -->
    <script src="~/lib/jquery/dist/jquery.min.js"></script>
    <script src="~/lib/bootstrap/dist/js/bootstrap.bundle.min.js"></script>
    <script src="https://kit.fontawesome.com/c58416c086.js" crossorigin="anonymous"></script>
    <script src="~/js/site.js" asp-append-version="true"></script>

    <!-- External Styles -->
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.1.1/css/all.min.css">
    <link rel="stylesheet" href="~/lib/bootstrap/dist/css/bootstrap.min.css" />
    <link rel="stylesheet" href="~/css/site.css" asp-append-version="true" />
    <link rel="stylesheet" href="~/EventEatsQuotify.styles.css" asp-append-version="true" />

    <style>

    </style>
</head>
<body>
    <header class="position-sticky top-0 custom-navbar">
        <nav class="navbar navbar-expand-sm navbar-toggleable-sm navbar-light bg-white border-bottom box-shadow mb-3">
            <!-- Navbar content -->
            <div class="container-fluid">
                <a class="navbar-brand text-danger fw-bold fs-4" style="font-family:'Segoe UI', Tahoma, Geneva, Verdana, sans-serif" asp-area="" asp-controller="Home" asp-action="Index">EventEats Quotify</a>
                <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target=".navbar-collapse" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
                    <span class="navbar-toggler-icon"></span>
                </button>
                <div class="navbar-collapse collapse d-sm-inline-flex justify-content-between">
                    <ul class="navbar-nav flex-grow-1">
                        @if (!User.IsInRole("Admin"))
                        {
                            @if (User.IsInRole("Vendor"))
                            {
                                <li class="nav-item">
                                    <a class="nav-link text-dark" asp-area="" asp-controller="Vendor" asp-action="VendorDashboard"><i class="fas fa-home"></i> Home</a>
                                </li>
                                <li class="nav-item">
                                    <a class="nav-link text-dark" asp-area="" asp-controller="ContactUs" asp-action="Contact"><i class="fas fa-envelope"></i> Contact</a>
                                </li>
                            }
                            else if (User.IsInRole("Customer"))
                            {
                                <li class="nav-item">
                                    <a class="nav-link text-dark" asp-area="" asp-controller="Customer" asp-action="CustomerDashboard"><i class="fas fa-home"></i> Home</a>
                                </li>
                                <li class="nav-item">
                                    <a class="nav-link text-dark" asp-area="" asp-controller="ContactUs" asp-action="Contact"><i class="fas fa-envelope"></i> Feedback</a>
                                </li>
                            }
                           

                        @*     <li class="nav-item">
                                <a class="nav-link text-dark" asp-controller="FoodItem" asp-action="Index"><i class="fas fa-utensils"></i> Order Now</a>
                            </li>
                            <li class="nav-item">
                                <a class="nav-link text-dark" asp-area=""><i class="fas fa-comment-alt"></i>Feedbacks</a>
                            </li> *@
                           @*  <li class="nav-item">
                                <a class="nav-link text-dark" asp-area="" asp-controller="ContactUs" asp-action="Contact"><i class="fas fa-envelope"></i> Contact</a>
                            </li> *@
                        }
                    </ul>
                    @if (!User.Identity.IsAuthenticated)
                    {
                        <div>
                            <a class="nav-link fw-bold text-pink-menu" asp-controller="Account" asp-action="Login"><i class="fas fa-sign-in-alt"></i> LogIn/SignUp</a>
                        </div>
                    }
                    else
                    {
                        @if (User.IsInRole("Vendor"))
                        {
                            <div class="d-flex align-items-center">
                                <a class="nav-link vendor-link text-dark" asp-controller="VendorProfile" asp-action="Index">Hi, @User.Identity.Name! <i class="fas fa-user"></i></a>
                                <a class="nav-link fw-bold text-pink-menu" asp-controller="Account" asp-action="LogOut"><i class="fas fa-sign-out-alt"></i> SignOut</a>
                            </div>
                        }
                        else if (User.IsInRole("Customer"))
                        {
                            <div class="d-flex align-items-center">
                                <a class="nav-link customer-link text-dark" asp-controller="CustomerProfile" asp-action="Index">Hi, @User.Identity.Name! <i class="fas fa-user"></i></a>
                                <a class="nav-link fw-bold text-pink-menu" asp-controller="Account" asp-action="LogOut"><i class="fas fa-sign-out-alt"></i> SignOut</a>
                            </div>
                        }
                        else
                        {
                            <div class="d-flex align-items-center">
                                <a class="nav-link customer-link text-dark" asp-controller="Admin" asp-action="AdminDashboard">Hi, @User.Identity.Name! <i class="fas fa-user"></i></a>
                                <a class="nav-link fw-bold text-pink-menu" asp-controller="Account" asp-action="LogOut"><i class="fas fa-sign-out-alt"></i> SignOut</a>
                            </div>
                        }
                    }
                </div>
            </div>
        </nav>
    </header>
    <div class="container">
        <main role="main" class="pb-3">
            @RenderBody()
        </main>
    </div>

    <footer class="border-top bg-light py-5 custom-footer">
        <div class="container">
            <div class="row">
                <div class="col-md-4">
                    <h4 class="mb-4 footer-heading">EventEats Quotify</h4>
                    <p class="text-muted">Your go-to platform for event catering and quotations.</p>
                </div>
                <div class="col-md-4">
                    <h4 class="mb-4 footer-heading">Contact Us</h4>
                    <ul class="list-unstyled">
                        <li>
                            <i class="fab fa-instagram me-2 text-primary"></i>
                            <a href="#">Instagram</a>
                        </li>
                        <li>
                            <i class="fab fa-whatsapp me-2 text-success"></i>
                            <a href="#">WhatsApp</a>
                        </li>
                        <li>
                            <i class="fab fa-facebook me-2 text-primary"></i>
                            <a href="#">Facebook</a>
                        </li>
                        <li>
                            <i class="far fa-envelope me-2 text-secondary"></i>
                            <a href="#">Mail</a>
                        </li>
                    </ul>
                </div>
                <div class="col-md-4">
                    <h4 class="mb-4 footer-heading">Address</h4>
                    <address class="text-muted">XYZ, Block A, Karachi, Pakistan</address>
                </div>
            </div>
            <div class="mt-4 text-center">
                <p class="mb-0">© 2024 EventEats Quotify. All rights reserved.</p>
                <p class="mb-0">Powered By <strong class="text-primary">CATERINGEXPERTS</strong></p>
            </div>
        </div>
    </footer>

    @await RenderSectionAsync("Scripts", required: false)
    <script>
        @using System.Security.Claims

            document.addEventListener('DOMContentLoaded', function () {
                var userRole = '@User.FindFirstValue("http://schemas.microsoft.com/ws/2008/06/identity/claims/role")';

                if (userRole === 'Vendor') {
                    document.querySelectorAll('.customer-link').forEach(function (el) {
                        el.style.display = 'none';
                    });
                } else if (userRole === 'Customer') {
                    document.querySelectorAll('.vendor-link').forEach(function (el) {
                        el.style.display = 'none';
                    });
                }
            });
    </script>
</body>
</html>

